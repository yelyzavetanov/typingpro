import React from "react";
import s from "./PreHeader.module.css";

const PreHeader = () => {
    return(
        <div className={s.preHeader}>no text</div>
    );
}

export default PreHeader;